"""
Shared runtime primitives for YATSEE.

This package holds configuration loading, path resolution, logging helpers,
filesystem discovery, tracker helpers, hashing helpers, and project-specific
exceptions used by CLI commands and pipeline stages.
"""